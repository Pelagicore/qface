image: "python:3.5"

stages:
  - test

test:
  stage: test
  script:
    - apt-get update -qy
    - apt-get install -y python-dev python-pip
    - pip install -r requirements.txt    \
    - ./cli.py test_ci
